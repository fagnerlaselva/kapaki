---
phase: 03-paginas-de-servico
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/pages/assistencia-tecnica.vue
  - app/pages/acessorios.vue
  - app/pages/aparelhos.vue
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-03
  - SEO-01
  - SEO-03
  - SEO-04
  - PERF-04

must_haves:
  truths:
    - "Visiting /assistencia-tecnica shows H1 'Assistencia Tecnica de Celular em Tatui' and a WhatsApp CTA"
    - "Visiting /acessorios shows H1 'Acessorios para Celular em Tatui' and a WhatsApp CTA"
    - "Visiting /aparelhos shows H1 'Celulares Novos e Seminovos em Tatui' and a WhatsApp CTA"
    - "/assistencia-tecnica has a FAQ section with 3 expandable questions"
    - "/assistencia-tecnica SSR HTML contains FAQPage JSON-LD schema"
    - "Each service page has unique useServerSeoMeta with title, description, ogImage (absolute URL)"
    - "/sitemap.xml includes all 4 routes"
    - "/robots.txt is accessible and allows crawling"
  artifacts:
    - path: "app/pages/assistencia-tecnica.vue"
      provides: "Assistencia tecnica service page with FAQ and FAQPage schema"
      contains: "FAQPage"
    - path: "app/pages/acessorios.vue"
      provides: "Acessorios service page with product highlights"
      contains: "useServerSeoMeta"
    - path: "app/pages/aparelhos.vue"
      provides: "Aparelhos service page with differentials"
      contains: "useServerSeoMeta"
  key_links:
    - from: "app/pages/assistencia-tecnica.vue"
      to: "app/components/FaqAccordion.vue"
      via: "component usage with faqs prop"
      pattern: "FaqAccordion"
    - from: "app/pages/assistencia-tecnica.vue"
      to: "app/constants/faq.ts"
      via: "import FAQS constant"
      pattern: "FAQS"
    - from: "app/pages/assistencia-tecnica.vue"
      to: "app/composables/useWhatsApp.ts"
      via: "useWhatsApp('assistencia')"
      pattern: "useWhatsApp"
    - from: "app/pages/acessorios.vue"
      to: "app/components/ServiceHeroSection.vue"
      via: "component usage"
      pattern: "ServiceHeroSection"
    - from: "app/pages/aparelhos.vue"
      to: "app/components/CtaFinalSection.vue"
      via: "component usage for final CTA"
      pattern: "CtaFinalSection"
---

<objective>
Create the three service pages (/assistencia-tecnica, /acessorios, /aparelhos) with unique content, SEO meta, FAQ schema, and verify sitemap/robots.

Purpose: These are the core deliverables of Phase 3 — each page targets local SEO keywords and drives WhatsApp conversions for its specific service.

Output: 3 new page files, each with useServerSeoMeta, WhatsApp CTAs, and service-specific content from kapaki.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paginas-de-servico/03-RESEARCH.md
@.planning/phases/03-paginas-de-servico/03-01-SUMMARY.md
@kapaki.md (sections 9 and 10 — EXACT copy, meta tags, FAQ, service lists)
@app/pages/index.vue (pattern reference for useServerSeoMeta + page assembly)
@app/composables/useWhatsApp.ts (ServiceKey types available)
@app/components/CtaFinalSection.vue (reusable CTA component)
@app.config.ts (business data)
@nuxt.config.ts (site.url for absolute og:image)

<interfaces>
<!-- Components created by Plan 01 — use these directly -->

From app/constants/faq.ts:
```typescript
export interface FaqItem {
  question: string
  answer: string
}
export const FAQS: Record<string, FaqItem[]>
// FAQS.assistencia has 3 items
```

From app/components/ServiceHeroSection.vue:
```typescript
defineProps<{
  title: string        // H1 text with city keyword
  subtitle: string     // Supporting text
  whatsappUrl: string  // Service-specific WA link
  ctaLabel: string     // Button label
}>()
```

From app/components/FaqAccordion.vue:
```typescript
defineProps<{ faqs: FaqItem[] }>()
// Renders <details>/<summary> accordion with "Perguntas frequentes" heading
```

From app/components/CtaFinalSection.vue (existing):
```typescript
defineProps<{ whatsappUrl: string }>()
// Renders yellow full-width CTA section
```

From app/composables/useWhatsApp.ts:
```typescript
type ServiceKey = 'assistencia' | 'acessorios' | 'aparelhos' | 'geral'
export function useWhatsApp(service: ServiceKey): { whatsappUrl: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /assistencia-tecnica page with FAQ section and FAQPage JSON-LD schema</name>
  <files>app/pages/assistencia-tecnica.vue</files>
  <read_first>
    - kapaki.md (section 9 /assistencia-tecnica block — exact meta title, description, H1, service list, FAQ)
    - app/pages/index.vue (pattern for useServerSeoMeta + component assembly)
    - app/composables/useWhatsApp.ts (useWhatsApp('assistencia') call pattern)
    - .planning/phases/03-paginas-de-servico/03-RESEARCH.md (Pattern 1 for page composition, Pattern 3 for FAQPage schema)
    - .planning/phases/03-paginas-de-servico/03-01-SUMMARY.md (confirm Plan 01 outputs)
    - app/constants/faq.ts (FaqItem type and FAQS.assistencia data)
    - app/components/ServiceHeroSection.vue (props interface)
    - app/components/FaqAccordion.vue (props interface)
    - app/components/CtaFinalSection.vue (props interface)
  </read_first>
  <action>
    Create `app/pages/assistencia-tecnica.vue` following the exact page composition pattern from 03-RESEARCH.md Pattern 1.

    **Script setup:**

    1. Import `{ FAQS }` from `~/constants/faq` (explicit import needed for constants, not auto-imported)
    2. Call `useWhatsApp('assistencia')` to get whatsappUrl
    3. Get FAQ data: `const faqs = FAQS.assistencia`

    4. Call `useServerSeoMeta` with EXACT values from kapaki.md section 9:
    ```typescript
    useServerSeoMeta({
      title: 'Assistencia Tecnica de Celular em Tatui-SP | Kapaki',
      description: 'Conserto de celular em Tatui com agilidade e garantia. Troca de tela, bateria, manutencao geral. Fale agora com a Kapaki!',
      ogTitle: 'Assistencia Tecnica de Celular em Tatui | Kapaki',
      ogDescription: 'Troca de tela, bateria e consertos com garantia em Tatui-SP.',
      ogImage: 'https://kapaki.com.br/og-image.jpg',
      twitterCard: 'summary_large_image',
    })
    ```

    5. Inject FAQPage JSON-LD schema (per SEO-03 — ONLY on this page, NOT on acessorios or aparelhos):
    ```typescript
    useHead({
      script: [{
        type: 'application/ld+json',
        innerHTML: JSON.stringify({
          '@context': 'https://schema.org',
          '@type': 'FAQPage',
          mainEntity: faqs.map(faq => ({
            '@type': 'Question',
            name: faq.question,
            acceptedAnswer: {
              '@type': 'Answer',
              text: faq.answer,
            },
          })),
        }),
      }],
    })
    ```

    **Template structure:**
    ```vue
    <template>
      <div>
        <ServiceHeroSection
          title="Assistencia Tecnica de Celular em Tatui"
          subtitle="Troca de tela, bateria e consertos em geral com garantia."
          :whatsapp-url="whatsappUrl"
          cta-label="Solicitar orcamento"
        />

        <!-- Service list section (per kapaki.md section 9) -->
        <section class="py-16 px-4 bg-surface">
          <div class="max-w-4xl mx-auto">
            <h2 class="font-heading text-2xl font-bold text-text mb-8">Nossos servicos</h2>
            <ul class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <!-- 5 items from kapaki.md, each with icon -->
              <li> Troca de tela </li>
              <li> Troca de bateria </li>
              <li> Recuperacao de celular molhado </li>
              <li> Manutencao geral </li>
              <li> Desbloqueio </li>
            </ul>
          </div>
        </section>

        <FaqAccordion :faqs="faqs" />
        <CtaFinalSection :whatsapp-url="whatsappUrl" />
      </div>
    </template>
    ```

    Each `<li>` should have styling: `class="flex items-center gap-3 bg-background p-4 rounded-lg border border-border"` with a Lucide icon (import `Wrench` or `CheckCircle` from `lucide-vue-next`). Use the same icon for all items or pick a fitting one per service. The exact 5 service items from kapaki.md section 9 are:
    - Troca de tela
    - Troca de bateria
    - Recuperacao de celular molhado
    - Manutencao geral
    - Desbloqueio

    The page calls useWhatsApp('assistencia') — composable call at page level, passed as prop to ServiceHeroSection and CtaFinalSection.
  </action>
  <verify>
    <automated>
      # Verify page structure
      grep -q "Assistencia Tecnica de Celular em Tatui-SP | Kapaki" app/pages/assistencia-tecnica.vue && \
      grep -q "useServerSeoMeta" app/pages/assistencia-tecnica.vue && \
      grep -q "FAQPage" app/pages/assistencia-tecnica.vue && \
      grep -q "application/ld+json" app/pages/assistencia-tecnica.vue && \
      grep -q "FaqAccordion" app/pages/assistencia-tecnica.vue && \
      grep -q "ServiceHeroSection" app/pages/assistencia-tecnica.vue && \
      grep -q "CtaFinalSection" app/pages/assistencia-tecnica.vue && \
      grep -q "useWhatsApp" app/pages/assistencia-tecnica.vue && \
      grep -q "https://kapaki.com.br/og-image.jpg" app/pages/assistencia-tecnica.vue && \
      grep -q "Troca de tela" app/pages/assistencia-tecnica.vue && \
      grep -q "Desbloqueio" app/pages/assistencia-tecnica.vue && \
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - Page has H1 "Assistencia Tecnica de Celular em Tatui" (contains city keyword per SEO requirement)
    - Meta title is exactly "Assistencia Tecnica de Celular em Tatui-SP | Kapaki"
    - Meta description matches kapaki.md section 9
    - ogImage is "https://kapaki.com.br/og-image.jpg" (absolute URL)
    - FAQPage JSON-LD schema is present with 3 Question/Answer entities
    - Service list contains exactly 5 items from kapaki.md: Troca de tela, Troca de bateria, Recuperacao de celular molhado, Manutencao geral, Desbloqueio
    - FaqAccordion receives faqs prop from FAQS.assistencia
    - CtaFinalSection receives whatsappUrl prop
    - useWhatsApp('assistencia') called at page level only
    - No useAppConfig or useWhatsApp calls inside template components
  </acceptance_criteria>
  <done>/assistencia-tecnica page renders with unique H1, FAQ accordion, FAQPage JSON-LD in SSR HTML, 5 service items, and WhatsApp CTA</done>
</task>

<task type="auto">
  <name>Task 2: Create /acessorios and /aparelhos pages, then verify sitemap and robots.txt</name>
  <files>app/pages/acessorios.vue, app/pages/aparelhos.vue</files>
  <read_first>
    - kapaki.md (section 9 /acessorios and /aparelhos blocks — exact meta, H1, product list, differentials)
    - app/pages/assistencia-tecnica.vue (pattern just created in Task 1)
    - app/pages/index.vue (useServerSeoMeta pattern)
    - app/composables/useWhatsApp.ts (ServiceKey: 'acessorios' and 'aparelhos')
    - app/components/ServiceHeroSection.vue (props)
    - app/components/CtaFinalSection.vue (props)
    - .planning/phases/03-paginas-de-servico/03-RESEARCH.md (Pattern 7 for sitemap zero-config)
    - nuxt.config.ts (site.url for og:image)
  </read_first>
  <action>
    **1. Create `app/pages/acessorios.vue`** (per SERV-02):

    Script setup:
    - `useWhatsApp('acessorios')` for whatsappUrl
    - `useServerSeoMeta` with EXACT values from kapaki.md section 9:
    ```typescript
    useServerSeoMeta({
      title: 'Acessorios para Celular em Tatui-SP | Kapaki',
      description: 'Capinhas, peliculas, carregadores e fones em Tatui. Variedade e preco justo na Kapaki Tatui.',
      ogTitle: 'Acessorios para Celular em Tatui | Kapaki',
      ogDescription: 'Capinhas, peliculas, carregadores e fones com preco justo em Tatui-SP.',
      ogImage: 'https://kapaki.com.br/og-image.jpg',
      twitterCard: 'summary_large_image',
    })
    ```

    Template:
    ```vue
    <template>
      <div>
        <ServiceHeroSection
          title="Acessorios para Celular em Tatui"
          subtitle="Capinhas, peliculas, carregadores, fones e muito mais com preco justo."
          :whatsapp-url="whatsappUrl"
          cta-label="Ver acessorios"
        />

        <!-- Product highlights section (per kapaki.md section 9) -->
        <section class="py-16 px-4 bg-surface">
          <div class="max-w-4xl mx-auto">
            <h2 class="font-heading text-2xl font-bold text-text mb-8">Produtos em destaque</h2>
            <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
              <!-- 6 items from kapaki.md -->
            </ul>
          </div>
        </section>

        <CtaFinalSection :whatsapp-url="whatsappUrl" />
      </div>
    </template>
    ```

    The 6 product categories from kapaki.md section 9:
    - Capinhas (todas as marcas)
    - Peliculas (vidro e comum)
    - Carregadores (originais e compativeis)
    - Fones de ouvido
    - Suportes veiculares
    - Cabos e adaptadores

    Each `<li>` styled as a card: `class="bg-background p-5 rounded-lg border border-border text-center"` with the category name as `<h3 class="font-heading font-semibold text-text">` and a brief description as `<p class="text-text-muted text-sm mt-2">`.

    NO FaqAccordion on this page (per kapaki.md — only /assistencia-tecnica has FAQ).
    NO FAQPage schema on this page.

    **2. Create `app/pages/aparelhos.vue`** (per SERV-03):

    Script setup:
    - `useWhatsApp('aparelhos')` for whatsappUrl
    - `useServerSeoMeta` with EXACT values from kapaki.md section 9:
    ```typescript
    useServerSeoMeta({
      title: 'Celulares Novos e Seminovos em Tatui-SP | Kapaki',
      description: 'Compre seu smartphone com seguranca em Tatui. Aparelhos com garantia e procedencia na Kapaki.',
      ogTitle: 'Celulares Novos e Seminovos em Tatui | Kapaki',
      ogDescription: 'Smartphones com garantia e procedencia em Tatui-SP.',
      ogImage: 'https://kapaki.com.br/og-image.jpg',
      twitterCard: 'summary_large_image',
    })
    ```

    Template:
    ```vue
    <template>
      <div>
        <ServiceHeroSection
          title="Celulares Novos e Seminovos em Tatui"
          subtitle="Smartphones com procedencia verificada e garantia."
          :whatsapp-url="whatsappUrl"
          cta-label="Ver aparelhos"
        />

        <!-- Differentials section (per kapaki.md section 9) -->
        <section class="py-16 px-4 bg-surface">
          <div class="max-w-4xl mx-auto">
            <h2 class="font-heading text-2xl font-bold text-text mb-8">Por que comprar na Kapaki?</h2>
            <ul class="grid grid-cols-1 sm:grid-cols-2 gap-6">
              <!-- 4 differentials from kapaki.md -->
            </ul>
          </div>
        </section>

        <CtaFinalSection :whatsapp-url="whatsappUrl" />
      </div>
    </template>
    ```

    The 4 differentials from kapaki.md section 9:
    - Procedencia verificada
    - Garantia em todos os aparelhos
    - Seminovos revisados
    - Parcelamento disponivel

    Each `<li>` styled as: `class="flex items-start gap-4 bg-background p-5 rounded-lg border border-border"` with a CheckCircle or ShieldCheck icon from lucide-vue-next and the text.

    NO FaqAccordion on this page.
    NO FAQPage schema on this page.

    **3. Verify sitemap.xml and robots.txt** (per SEO-04):

    After creating the pages, start the dev server and verify:
    - `curl -s http://localhost:3000/sitemap.xml` returns XML with all 4 routes (/, /assistencia-tecnica, /acessorios, /aparelhos)
    - `curl -s http://localhost:3000/robots.txt` returns a valid robots.txt

    These are auto-generated by @nuxtjs/seo (already installed) — no config changes needed. If either is missing, check nuxt.config.ts for any incorrect robots/sitemap config.

    Do NOT create a `public/robots.txt` file — per PITFALLS.md Pitfall 11, `public/_robots.txt` already exists and the module handles it.
  </action>
  <verify>
    <automated>
      # Verify acessorios page
      grep -q "Acessorios para Celular em Tatui-SP | Kapaki" app/pages/acessorios.vue && \
      grep -q "useServerSeoMeta" app/pages/acessorios.vue && \
      grep -q "ServiceHeroSection" app/pages/acessorios.vue && \
      grep -q "CtaFinalSection" app/pages/acessorios.vue && \
      grep -q "https://kapaki.com.br/og-image.jpg" app/pages/acessorios.vue && \
      grep -q "Capinhas" app/pages/acessorios.vue && \
      grep -q "useWhatsApp" app/pages/acessorios.vue && \
      ! grep -q "FaqAccordion" app/pages/acessorios.vue && \
      ! grep -q "FAQPage" app/pages/acessorios.vue && \
      # Verify aparelhos page
      grep -q "Celulares Novos e Seminovos em Tatui-SP | Kapaki" app/pages/aparelhos.vue && \
      grep -q "useServerSeoMeta" app/pages/aparelhos.vue && \
      grep -q "ServiceHeroSection" app/pages/aparelhos.vue && \
      grep -q "CtaFinalSection" app/pages/aparelhos.vue && \
      grep -q "https://kapaki.com.br/og-image.jpg" app/pages/aparelhos.vue && \
      grep -q "Procedencia verificada" app/pages/aparelhos.vue && \
      grep -q "useWhatsApp" app/pages/aparelhos.vue && \
      ! grep -q "FaqAccordion" app/pages/aparelhos.vue && \
      ! grep -q "FAQPage" app/pages/aparelhos.vue && \
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - /acessorios page has H1 "Acessorios para Celular em Tatui" (city keyword present)
    - /acessorios meta title is exactly "Acessorios para Celular em Tatui-SP | Kapaki"
    - /acessorios lists 6 product categories from kapaki.md: Capinhas, Peliculas, Carregadores, Fones de ouvido, Suportes veiculares, Cabos e adaptadores
    - /acessorios has NO FaqAccordion and NO FAQPage schema
    - /aparelhos page has H1 "Celulares Novos e Seminovos em Tatui" (city keyword present)
    - /aparelhos meta title is exactly "Celulares Novos e Seminovos em Tatui-SP | Kapaki"
    - /aparelhos lists 4 differentials from kapaki.md: Procedencia verificada, Garantia em todos os aparelhos, Seminovos revisados, Parcelamento disponivel
    - /aparelhos has NO FaqAccordion and NO FAQPage schema
    - All ogImage values use absolute URL "https://kapaki.com.br/og-image.jpg"
    - useWhatsApp called at page level only (acessorios uses 'acessorios', aparelhos uses 'aparelhos')
    - /sitemap.xml contains all 4 routes
    - /robots.txt is accessible
  </acceptance_criteria>
  <done>/acessorios and /aparelhos pages render with unique H1s, service-specific content, WhatsApp CTAs; sitemap.xml lists all 4 routes; robots.txt accessible</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `npm run dev` — no errors
2. Visit each URL and verify:
   - http://localhost:3000/assistencia-tecnica — H1, FAQ, service list, CTA
   - http://localhost:3000/acessorios — H1, product grid, CTA
   - http://localhost:3000/aparelhos — H1, differentials, CTA
3. `curl -s http://localhost:3000/assistencia-tecnica | grep -o 'FAQPage'` — FAQPage schema in SSR HTML
4. `curl -s http://localhost:3000/acessorios | grep -c 'FAQPage'` — should return 0 (no FAQ on this page)
5. `curl -s http://localhost:3000/aparelhos | grep -c 'FAQPage'` — should return 0 (no FAQ on this page)
6. `curl -s http://localhost:3000/assistencia-tecnica | grep -o 'ElectronicsRepair'` — global schema present
7. `curl -s http://localhost:3000/sitemap.xml` — all 4 URLs listed
8. `curl -s http://localhost:3000/robots.txt` — valid response
</verification>

<success_criteria>
- 3 service pages exist with unique H1s containing "em Tatui" city keyword
- Each page has useServerSeoMeta with unique title/description from kapaki.md section 9
- All og:image values are absolute URLs
- FAQPage schema exists ONLY on /assistencia-tecnica
- No FaqAccordion on /acessorios or /aparelhos
- ElectronicsRepair schema (from Plan 01) renders on all pages via default.vue
- sitemap.xml auto-generated with all 4 routes
- robots.txt accessible
- All WhatsApp buttons link to service-specific messages
</success_criteria>

<output>
After completion, create `.planning/phases/03-paginas-de-servico/03-02-SUMMARY.md`
</output>
