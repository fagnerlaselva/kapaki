---
phase: 03-paginas-de-servico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/constants/faq.ts
  - app/components/ServiceHeroSection.vue
  - app/components/FaqAccordion.vue
  - app/layouts/default.vue
  - app/pages/index.vue
  - app/assets/css/main.css
  - public/og-image.jpg
autonomous: true
requirements:
  - SERV-04
  - SEO-02
  - SEO-05
  - PERF-04
  - PERF-05

must_haves:
  truths:
    - "ServiceHeroSection renders an H1, subtitle, and WhatsApp CTA button"
    - "FaqAccordion renders expandable details/summary elements from a faqs prop"
    - "ElectronicsRepair JSON-LD appears in the HTML of every page via default.vue"
    - "og:image in index.vue uses absolute URL https://kapaki.com.br/og-image.jpg"
    - "public/og-image.jpg exists and is served at /og-image.jpg"
    - "A --color-border token exists in main.css for FaqAccordion borders"
  artifacts:
    - path: "app/constants/faq.ts"
      provides: "FaqItem interface and FAQS constant with assistencia FAQ data"
      exports: ["FaqItem", "FAQS"]
    - path: "app/components/ServiceHeroSection.vue"
      provides: "Shared hero for service pages with H1, subtitle, WA CTA"
      contains: "defineProps"
    - path: "app/components/FaqAccordion.vue"
      provides: "Accordion component using native details/summary"
      contains: "details"
    - path: "app/layouts/default.vue"
      provides: "Global ElectronicsRepair JSON-LD schema"
      contains: "ElectronicsRepair"
    - path: "app/pages/index.vue"
      provides: "Fixed ogImage with absolute URL"
      contains: "https://kapaki.com.br/og-image.jpg"
    - path: "public/og-image.jpg"
      provides: "Placeholder OG image file"
  key_links:
    - from: "app/components/FaqAccordion.vue"
      to: "app/constants/faq.ts"
      via: "import type { FaqItem }"
      pattern: "FaqItem"
    - from: "app/layouts/default.vue"
      to: "app.config.ts"
      via: "useAppConfig() for NAP data in schema"
      pattern: "useAppConfig"
---

<objective>
Create the shared components, data constants, and SEO infrastructure needed by all three service pages.

Purpose: Service pages depend on ServiceHeroSection, FaqAccordion, faq.ts data, and the global ElectronicsRepair schema. Building these first prevents the "scavenger hunt" anti-pattern in Plan 02.

Output: 4 new files (faq.ts, ServiceHeroSection.vue, FaqAccordion.vue, og-image.jpg), 3 modified files (default.vue, index.vue, main.css)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paginas-de-servico/03-RESEARCH.md
@kapaki.md (sections 9 and 10 — service page specs, FAQ content, meta tags, schema)
@app/layouts/default.vue
@app/pages/index.vue
@app/assets/css/main.css
@app/composables/useWhatsApp.ts
@app/components/HeroSection.vue (reference pattern for ServiceHeroSection)
@app/components/CtaFinalSection.vue (reference for presentational pattern)
@app.config.ts
@nuxt.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ data, ServiceHeroSection, FaqAccordion, og-image placeholder, and add border color token</name>
  <files>app/constants/faq.ts, app/components/ServiceHeroSection.vue, app/components/FaqAccordion.vue, public/og-image.jpg, app/assets/css/main.css</files>
  <read_first>
    - kapaki.md (section 9 for exact FAQ Q&A pairs)
    - app/components/HeroSection.vue (pattern reference for ServiceHeroSection)
    - app/components/CtaFinalSection.vue (presentational pattern reference)
    - app/assets/css/main.css (current theme tokens)
    - app/composables/useWhatsApp.ts (ServiceKey type for reference)
    - .planning/phases/03-paginas-de-servico/03-RESEARCH.md (Pattern 1, 6)
  </read_first>
  <action>
    **1. Create `app/constants/faq.ts`** with the exact FAQ content from kapaki.md section 9:

    Export a `FaqItem` interface with `question: string` and `answer: string` fields.
    Export a `FAQS` constant of type `Record<string, FaqItem[]>` with one key:
    - `assistencia` with exactly 3 items:
      - Q: "Quanto tempo demora o conserto?" / A: "A maioria dos reparos e feita no mesmo dia. Tela e bateria geralmente em 1-2 horas."
      - Q: "Tem garantia no servico?" / A: "Sim. Todos os nossos servicos possuem garantia."
      - Q: "Preciso agendar?" / A: "Nao e necessario. Pode vir direto na loja ou chamar no WhatsApp antes."

    Note: Use unaccented Portuguese (project convention — see existing constants/services.ts).

    **2. Create `app/components/ServiceHeroSection.vue`** (per SERV-04, PERF-04, PERF-05):

    Props interface:
    ```typescript
    defineProps<{
      title: string        // H1 text with city keyword
      subtitle: string     // Supporting text
      whatsappUrl: string  // Service-specific WA link
      ctaLabel: string     // Button label (e.g., "Solicitar orcamento")
    }>()
    ```

    Template structure:
    - `<section class="bg-background py-20 lg:py-28 px-4">` — dark gradient background, NO raster image (real photos pending per STATE.md)
    - `<div class="max-w-4xl mx-auto text-center">` — centered layout
    - `<h1 class="font-heading text-3xl lg:text-5xl font-bold text-text mb-4">` with `{{ title }}`
    - `<p class="text-lg text-text-muted mb-8 max-w-2xl mx-auto">` with `{{ subtitle }}`
    - WhatsApp CTA button: `<a :href="whatsappUrl" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 bg-primary text-background font-heading font-semibold px-8 py-4 rounded-lg hover:bg-primary-hover transition-colors">`
    - Import `MessageCircle` from `lucide-vue-next` for the button icon
    - This is a presentational component — NO composable calls inside

    Since no raster hero image is used (CSS gradient only), PERF-05 (eager loading) does not apply at this stage. When real images are added later, they must use `<NuxtImg loading="eager" fetchpriority="high">`.

    **3. Create `app/components/FaqAccordion.vue`** (per SERV-01):

    Props: `defineProps<{ faqs: FaqItem[] }>()` — import `FaqItem` type from `~/constants/faq`.

    Template using native `<details>/<summary>` (zero JS, SSR-safe per research Pattern 6):
    - `<section class="py-16 px-4 bg-background">`
    - `<div class="max-w-3xl mx-auto">`
    - `<h2 class="font-heading text-2xl font-bold text-text mb-8">Perguntas frequentes</h2>`
    - `<div class="space-y-3">` with `v-for="faq in faqs" :key="faq.question"`
    - Each item: `<details class="border border-border rounded-lg overflow-hidden group">`
    - `<summary class="flex justify-between items-center px-5 py-4 cursor-pointer font-heading font-semibold text-text list-none">` with `{{ faq.question }}` and `<ChevronDown class="size-5 text-text-muted transition-transform group-open:rotate-180" />`
    - `<p class="px-5 pb-4 text-text-muted text-sm leading-relaxed">{{ faq.answer }}</p>`
    - Import `ChevronDown` from `lucide-vue-next`
    - Presentational only — NO composable calls

    **4. Add `--color-border` token to `app/assets/css/main.css`:**

    Inside the existing `@theme { }` block, add: `--color-border: #2A2A2A;`
    This provides the `border-border` Tailwind class used by FaqAccordion. Place it after `--color-surface`.

    **5. Create `public/og-image.jpg` placeholder:**

    Generate a simple 1200x630 placeholder image. Use a Node.js one-liner or download a solid color placeholder. The image should be a solid rectangle — brand yellow (#F5C400) background is acceptable. This is a placeholder until real brand assets are provided.

    Approach: Create a minimal valid JPEG using Node.js sharp (if available) or use a simple HTTP download of a generated placeholder image. If neither works, create a minimal binary JPEG file. The key requirement is that `public/og-image.jpg` exists and is a valid image file.
  </action>
  <verify>
    <automated>
      # Verify faq.ts exports
      grep -q "export interface FaqItem" app/constants/faq.ts && \
      grep -q "export const FAQS" app/constants/faq.ts && \
      grep -c "question:" app/constants/faq.ts | grep -q "3" && \
      # Verify ServiceHeroSection
      grep -q "defineProps" app/components/ServiceHeroSection.vue && \
      grep -q "whatsappUrl" app/components/ServiceHeroSection.vue && \
      grep -q "<h1" app/components/ServiceHeroSection.vue && \
      # Verify FaqAccordion
      grep -q "details" app/components/FaqAccordion.vue && \
      grep -q "summary" app/components/FaqAccordion.vue && \
      grep -q "FaqItem" app/components/FaqAccordion.vue && \
      # Verify border token
      grep -q "\-\-color-border" app/assets/css/main.css && \
      # Verify og-image exists
      test -f public/og-image.jpg && \
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - app/constants/faq.ts exports FaqItem interface and FAQS constant with exactly 3 assistencia FAQ items matching kapaki.md section 9
    - app/components/ServiceHeroSection.vue has props: title, subtitle, whatsappUrl, ctaLabel; renders h1 and WhatsApp CTA; is presentational
    - app/components/FaqAccordion.vue uses native details/summary elements; receives faqs: FaqItem[] prop; imports ChevronDown from lucide-vue-next
    - app/assets/css/main.css contains --color-border token inside @theme block
    - public/og-image.jpg exists as a valid image file
  </acceptance_criteria>
  <done>All shared components and data files exist and follow established project patterns (presentational components, props-only, Tailwind theme tokens)</done>
</task>

<task type="auto">
  <name>Task 2: Add ElectronicsRepair schema to default.vue and fix index.vue og:image to absolute URL</name>
  <files>app/layouts/default.vue, app/pages/index.vue</files>
  <read_first>
    - app/layouts/default.vue (current content — must preserve existing code)
    - app/pages/index.vue (current content — find ogImage to fix)
    - app.config.ts (business data for schema)
    - kapaki.md (section 10 for LocalBusiness schema structure)
    - .planning/phases/03-paginas-de-servico/03-RESEARCH.md (Pattern 2 for ElectronicsRepair, Pattern 4 for absolute URL)
    - .planning/research/PITFALLS.md (Pitfall 9 for ElectronicsRepair type)
    - nuxt.config.ts (site.url value: https://kapaki.com.br)
  </read_first>
  <action>
    **1. Add ElectronicsRepair JSON-LD to `app/layouts/default.vue`** (per SEO-02):

    In the existing `<script setup>`, after the current `const config = useAppConfig()` line, add a `useHead` call that injects the ElectronicsRepair schema as JSON-LD. Use `ElectronicsRepair` type (NOT `LocalBusiness` — per PITFALLS.md Pitfall 9, it is the most specific schema.org type for a phone repair shop).

    The schema must use data from `useAppConfig()` to keep NAP consistent (per PITFALLS.md Pitfall 3):

    ```typescript
    const addr = config.address as { street: string; city: string; state: string; zip: string }

    useHead({
      script: [{
        type: 'application/ld+json',
        innerHTML: JSON.stringify({
          '@context': 'https://schema.org',
          '@type': 'ElectronicsRepair',
          name: config.siteName as string,
          description: 'Assistencia tecnica para celulares, acessorios e aparelhos em Tatui-SP',
          url: 'https://kapaki.com.br',
          telephone: config.phone as string,
          address: {
            '@type': 'PostalAddress',
            streetAddress: addr.street,
            addressLocality: addr.city,
            addressRegion: addr.state,
            postalCode: addr.zip,
            addressCountry: 'BR',
          },
          openingHoursSpecification: [
            {
              '@type': 'OpeningHoursSpecification',
              dayOfWeek: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'],
              opens: '09:00',
              closes: '18:00',
            },
            {
              '@type': 'OpeningHoursSpecification',
              dayOfWeek: ['Saturday'],
              opens: '09:00',
              closes: '13:00',
            },
          ],
          image: 'https://kapaki.com.br/og-image.jpg',
          priceRange: '$$',
          sameAs: [
            `https://www.instagram.com/${(config.instagram as string).replace('@', '')}/`,
          ],
          // geo coordinates pending — add when client provides lat/lng
        }),
      }],
    })
    ```

    IMPORTANT: Do NOT inject ElectronicsRepair in individual service pages — it goes ONLY in default.vue (per research anti-pattern: "Multiple ElectronicsRepair schemas").

    Keep ALL existing template and script code in default.vue intact (the slot, AppFooter props, footerWaUrl).

    **2. Fix og:image in `app/pages/index.vue`** (per SEO-05):

    Change line `ogImage: '/og-image.jpg',` to `ogImage: 'https://kapaki.com.br/og-image.jpg',`

    This fixes the pre-existing bug documented in research (Pitfall 1: og:image Relative URL Breaks WhatsApp Preview). The URL must be absolute for WhatsApp link previews to work.

    Do NOT change any other code in index.vue.
  </action>
  <verify>
    <automated>
      # Verify ElectronicsRepair schema in default.vue
      grep -q "ElectronicsRepair" app/layouts/default.vue && \
      grep -q "application/ld+json" app/layouts/default.vue && \
      grep -q "openingHoursSpecification" app/layouts/default.vue && \
      grep -q "sameAs" app/layouts/default.vue && \
      # Verify existing code preserved
      grep -q "AppFooter" app/layouts/default.vue && \
      grep -q "footerWaUrl" app/layouts/default.vue && \
      # Verify index.vue og:image is absolute
      grep -q "https://kapaki.com.br/og-image.jpg" app/pages/index.vue && \
      # Verify no relative og:image remains
      ! grep -q "ogImage: '/og-image.jpg'" app/pages/index.vue && \
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - default.vue contains a useHead call with script type="application/ld+json" containing "@type": "ElectronicsRepair"
    - Schema includes name, description, url, telephone, address (PostalAddress), openingHoursSpecification, image, priceRange, sameAs — all sourced from useAppConfig()
    - Schema is injected ONCE globally in default.vue — NOT in individual pages
    - Existing default.vue template (slot, AppFooter) is unchanged
    - index.vue ogImage value is "https://kapaki.com.br/og-image.jpg" (absolute, not relative)
  </acceptance_criteria>
  <done>ElectronicsRepair JSON-LD renders in SSR HTML on every page; index.vue og:image uses absolute URL for WhatsApp preview compatibility</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `npx nuxi typecheck` — no TypeScript errors
2. Run `npm run dev` and verify dev server starts without errors
3. `curl -s http://localhost:3000 | grep -o 'ElectronicsRepair'` — confirms schema in SSR HTML
4. `curl -s http://localhost:3000 | grep -o 'https://kapaki.com.br/og-image.jpg'` — confirms absolute og:image
5. Verify `public/og-image.jpg` is accessible at `http://localhost:3000/og-image.jpg`
</verification>

<success_criteria>
- All 4 new files exist: faq.ts, ServiceHeroSection.vue, FaqAccordion.vue, og-image.jpg
- ElectronicsRepair schema renders in SSR HTML on all pages
- index.vue og:image is absolute URL
- border color token exists in main.css
- Dev server runs without errors
- No composable calls inside ServiceHeroSection or FaqAccordion (presentational only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-paginas-de-servico/03-01-SUMMARY.md`
</output>
